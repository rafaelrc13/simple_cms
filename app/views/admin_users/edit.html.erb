<% @page_title = "Update Admin User" %>

<%= link_to("<< Back to List", {:action => 'list'}, :class => 'back_link') %>

<div class="admin_user edit">
	<h2>Update Admin User</h2>
	
	<%= form_for(:admin_user, :url => {:action => 'update', :id => @admin_user.id}) do |f| %>
	
	<%= render(:partial => "form", :locals => {:f => f}) %>
	
	<div class="form-buttons">
		<%= submit_tag("Update Admin User") %>
	</div>
	
	<% end %>
	
</div>

<br><%= link_to("<< Back to main menu", {:controller => 'access', :action => 'menu'}, :class => 'back-link') %>